<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        .container {
            max-width: 1440px;
            padding-inline: 1rem;
            margin-inline: auto;
        }
        .heading h1{
            text-align: center;
            padding: 2rem 0;
        }
        .form {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }
        .cs-flex {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
            margin-bottom: 1rem;
            max-width: 28rem;
            margin-inline: auto;
        }
        form#registerForm {
            width: 100%;
        }
        input {
            padding: 0.4rem;
        }
        .submit {
            text-align: center;
        }
        a{
            text-decoration: none;
            padding: 5px;
            background-color: #000000;
            color: #ffffff;
            border-radius: 5px;
            display: inline-block;
            margin-top: 10px;
        }
        .error{
            margin-top: 1rem;
        }
        .error p {
            color: red;
        }
    </style>
</head>
<body>
    <section>
        <div class="container">
            <div class="heading">
                <h1>Login</h1>
            </div>
            <div class="form">
                <form action="/login" method="POST" id="registerForm">
                    <div class="cs-flex">
                        <label for="email">Email or Username</label>
                        <input type="text" name="userName" id="email" placeholder="Enter your email or username">
                    </div>
                    <div class="cs-flex">
                        <label for="password">Password</label>
                        <input type="password" name="password" id="password" placeholder="Enter your password">
                    </div>
                    <div class="submit">
                        <input type="submit" value="Login">
                    </div>
                </form>
                <div>
                    <a href="/forgotPassword">Forgot password</a>
                    <a href="/registration">Register</a>
                </div>
                <div class="error">
                    <% if(query) {%>
                        <p><%= query %></p>
                        <% } %>
                </div>
            </div>
        </div>
    </section>
</body>
</html>